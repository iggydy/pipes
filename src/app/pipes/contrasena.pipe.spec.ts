import { ContrasenaPipe } from './contrasena.pipe';

describe('ConrasenaPipe', () => {
  it('create an instance', () => {
    const pipe = new ContrasenaPipe();
    expect(pipe).toBeTruthy();
  });
});
